// Create a function to update the content of the element whose id atter
function getArea(width, height){
    var el = document.getElementById( elementld: 'message');
    el.textContent = "Area is " + width*height;
}
//Call the function
getArea(width: 3, height: 5);