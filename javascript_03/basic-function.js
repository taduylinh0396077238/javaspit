// Create a variable called msg to hold a new message
var msg = 'sign up to receive our newsletter for 10% off!';
// Create a function to update the content of the element whose id attr
function updateMessage(){
    var el = document.getElementById(elementld: 'message');
    el.textContent = msg;
}
// Call the function
updateMessage();